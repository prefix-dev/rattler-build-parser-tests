diff --git a/pyproject.toml b/pyproject.toml
index 2d7737f..98377d5 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,5 +1,6 @@
 [build-system]
-requires = ["setuptools", "setuptools-rust"]
+# [conda-forge] avoid build-time rust
+requires = ["setuptools"]
 build-backend = "setuptools.build_meta"
 
 [project]
