context:
  name: mednet
  version: 2.0.2
  python_min: 3.11

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://files.pythonhosted.org/packages/98/9a/569edc4e67b14710cdef5b53ada6f312ac16fab5953a060c7fa570faf7ec/mednet-${{ version }}.tar.gz
  sha256: 312b476a012da7bb98c6b22703bc40a0296b3c2aa16db96febf457aad196ff3c

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - mednet = mednet.scripts.cli:cli

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - versioningit
    - pip
  run:
    - python >=${{ python_min }}
    - clapper >=1.3.4
    - click
    - colorlog
    - compact-json
    - credible >=1.0.2
    - grad-cam >=1.5.0
    - lightning >=2.2.0
    - loky
    - h5py
    - matplotlib-base
    - mlflow-skinny
    - numpy
    - peft  # needs transformers
    - transformers >=4.36.0  # avoids huggingface-hub conflicts
    - pillow
    - psutil
    - python-gitlab
    - scikit-image
    - scikit-learn
    - scipy
    - tabulate
    - tensorboard
    - timm
    - torchvision >=0.18.0
    - pytorch
    - torchio
    - torchmetrics
    - tqdm
    - versioningit
tests:
  - python:
      imports:
        - mednet
      pip_check: true
      python_version: ${{ python_min }}.*
  - files:
      source:
        - tests
    requirements:
      run:
        - pip
        - pytest
    script:
      - mednet --help
      - pytest --override-ini pythonpath=tests/src -sv tests/cli/test_cli.py

about:
  summary: Multi-task Library to Develop Computer-Aided Tools for Medical Data Analysis
  license: GPL-3.0-or-later
  license_file: LICENSES/GPL-3.0-or-later.txt
  homepage: https://gitlab.idiap.ch/medai/software/mednet
  repository: https://gitlab.idiap.ch/medai/software/mednet
  documentation: https://mednet.readthedocs.io

extra:
  recipe-maintainers:
    - anjos
