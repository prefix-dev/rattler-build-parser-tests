schema_version: 1

context:
  name: cvss
  version: "3.6"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/cvss-${{ version }}.tar.gz
  sha256: f21d18224efcd3c01b44ff1b37dec2e3208d29a6d0ce6c87a599c73c21ee1a99

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - cvss_calculator = cvss.cvss_calculator:main

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools
    - wheel
    - pip
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - cvss
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - python ${{ python_min }}.*
    script:
      - cvss_calculator --help

about:
  summary: "CVSS2/3/4 library with interactive calculator for Python 2 and Python 3"
  license: LGPL-3.0-only
  license_file: LICENSE
  homepage: https://github.com/RedHatProductSecurity/cvss

extra:
  recipe-maintainers:
    - thewchan
